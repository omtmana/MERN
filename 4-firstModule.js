const { __esModule } = require("react-copy-to-clipboard/lib/Component")

// local
const secret = 'SUPER SECRET'

// share (something like global)
const john = 'john'
const peter = 'peter'

module.exports = { john, peter }